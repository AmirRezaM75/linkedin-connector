# Linkedin Connector

## Installation

```
cp .env.example .env
```

Update database credentials to your needs:

```
DB_DATABASE=
DB_USERNAME=
DB_PASSWORD=
```

Build the image and run the container:

```
export UID=$UID
docker compose up -d
```
